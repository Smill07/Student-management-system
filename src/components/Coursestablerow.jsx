const Coursestablerow = ({ courses, students, deleteCourse, isAdmin }) => <>{courses.map((course) => <div key={course._id} className="flex border-b border-gray-100 px-6 py-4"><div className="w-2/5">{course.name}</div><div className="w-2/5 text-gray-500">{students.filter((student) => student.course === course.name).length}</div><div>{isAdmin && <button onClick={() => deleteCourse(course._id)} className="rounded-3xl border border-gray-200 px-3 py-1.5 hover:shadow-lg">Delete</button>}</div></div>)}</>
export default Coursestablerow
